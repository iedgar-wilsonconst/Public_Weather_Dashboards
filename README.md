# Construction Weather Dashboard PDFs

This repository is intended to be public and contain only shareable dashboard PDFs.

Stable GitHub Pages PDF URL pattern:

```text
https://<owner>.github.io/<repo>/pdfs/<ProjectLabel>_WeatherDashboard_latest.pdf
```

Enable GitHub Pages for this repository from the `main` branch root.
